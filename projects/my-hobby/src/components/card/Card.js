import styled from "styled-components";

const Heading = styled.h2`;
    color: black;
    `;

    const Card = () => {
        return (
            <div>
                <Heading>A list of my favorite games</Heading>
            </div>
        );
    };

    export default Card;
